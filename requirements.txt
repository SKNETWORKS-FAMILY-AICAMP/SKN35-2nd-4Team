# Streamlit Cloud 배포용 런타임 의존성.
#
# pyproject.toml 에는 분석·크롤링·CV 실습 패키지(mediapipe, ultralytics, opencv,
# selenium, pyautogui 등)가 함께 들어 있는데, 배포되는 앱은 그중 하나도 쓰지
# 않는다. 그대로 설치하면 빌드가 수 GB로 불어나고(설치본 기준 torch 501MB,
# cv2 138MB, mediapipe 99MB) pyautogui 처럼 디스플레이가 필요한 패키지는
# 헤드리스 환경에서 실패한다. 그래서 app/ 진입점에서 실제로 도달하는 모듈만
# 추려서 따로 관리한다.
#
# 추리는 방법(의존성 추가/제거 후 다시 돌려볼 것):
#   app/**.py 에서 시작해 src.*, ui.* 임포트를 따라가며 외부 패키지만 수집
#
# torch 는 CPU 휠을 쓴다 — 기본 인덱스의 CUDA 빌드는 2GB가 넘어 배포 한도를
# 넘긴다. 앱에서 torch 가 필요한 곳은 recommend_autoencoder.pt 하나뿐이다.
--extra-index-url https://download.pytorch.org/whl/cpu

streamlit>=1.62.0
pandas>=2.2
numpy>=1.26
pyarrow>=15.0          # features_v1.parquet 읽기
scikit-learn>=1.5      # reason_rf, recommend_knn
lightgbm>=4.3          # departure_lgbm
xgboost>=2.0           # strength_xgb
torch>=2.2             # recommend_autoencoder (CPU 휠)
joblib>=1.4            # 모델 pickle 로드

# Supabase(Postgres) — 화면이 로컬 parquet 대신 DB를 읽도록 전환할 때 필요.
# 지금은 app/ 이 storage 계층을 쓰지 않지만, 전환 시 주석만 풀면 되도록 남긴다.
# psycopg2-binary>=2.9
# python-dotenv>=1.0
